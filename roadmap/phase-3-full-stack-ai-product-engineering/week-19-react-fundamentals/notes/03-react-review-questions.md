# React Review Questions

Use these questions before moving to Week 20.

1. Why does the ticket collection live in `App` instead of `TicketList`?
2. Why does the form draft live in `TicketComposer` instead of `App`?
3. Why are stats derived instead of stored in separate state?
4. Why is `localStorage` synchronization handled in an effect?
5. Why does selection use an id instead of storing a copied ticket object?
6. What would break if filters and visible tickets were both stored independently?
7. Which components in the project are mostly presentational?
8. Which components are responsible for user input orchestration?
9. When would it be reasonable to split `App` further?
10. What would be the first changes if this app moved into a Next.js shell next week?
