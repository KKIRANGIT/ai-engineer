# Bottleneck Notes

Check these possible bottlenecks:

- AI provider latency
- background queue backlog
- noisy workspaces
- expensive repeated retrieval
- dashboard queries that scan too much data
- missing feature flags for risky launches
