# Identity, Session, and Authorization

Write one sentence for each of these terms without reusing the others:

- identity
- authentication
- session
- authorization

Then answer:

1. What request data should be available after the user signs in?
2. Which of those values should never come from the browser alone?
3. What can a signed-in user still be forbidden from doing?
