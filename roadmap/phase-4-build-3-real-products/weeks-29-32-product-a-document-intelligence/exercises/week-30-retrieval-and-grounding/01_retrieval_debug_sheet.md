# Retrieval Debug Sheet

For one question, record:

- the query
- top retrieved chunks
- why they were retrieved
- whether the answer was actually supported
- what should change if the result was weak

If you cannot explain why a chunk was retrieved, the retrieval layer is still too opaque.
