# Output Contract and Tools

For the workflow engine, define:

- the structured output shape
- the deterministic tools it depends on
- the validation that must happen before moving to the next state

If you cannot define the output contract clearly, the workflow is still too fuzzy to automate safely.
